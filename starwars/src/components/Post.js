import React from 'react';

export default function Post(e)
{

  return (
    <div>

      {`Name: ${e.char.name} `}
      {`Height: ${e.char.height} `}
      {`Mass: ${e.char.mass} `}
      {/* {`Name: ${e.char.name} `}
      {`Name: ${e.char.name} `}
      {`Name: ${e.char.name} `}
      {`Name: ${e.char.name} `} */





  }
    </div>
  )

}